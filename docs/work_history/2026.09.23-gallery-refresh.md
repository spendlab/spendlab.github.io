# SPEND website gallery refresh — 2026-09-23

## Request and baseline

The user requested that `https://app.spendlab.net/` use the same updated images as the iOS App Store. The public English home showed nine earlier 2026-09-07 App Store slides. Before this work, the website checkout was clean at `7122f8910a2fa05ce082c492ed31d420d115fcfa`, also the observed `origin/master`; the static site uses GitHub Pages with `CNAME=app.spendlab.net`. The privacy page and old policy redirect are outside this image change.

## Source and implementation

- Source is the final App Store en-US slide 01–09 package under `/Users/jaegeun/.codex/worktrees/release-candidate-0923/spend/docs/store_listing/2026.09.23/final-country-images/en-US/`, product SHA `bf1a964ed2c9d845cb8001a41b0062a4b213068a`, screenshot package commit `a9ed66eebaeec86b37341e1fb8ffed90af6177fd`. The old 2026-09-07 source manifest is retained as history; [the new manifest](landing-image-sources-2026.09.23.json) maps all nine exact source files to the existing home gallery paths and SHA-256 hashes.
- All nine PNGs were copied byte for byte to `images/` and verified against the App Store package's final asset manifest. Each is 1320×2868. Each `<img>` URL now has its new SHA-256 prefix as a query value, avoiding stale browser/CDN images. Slide 03 alt text now describes the new $90 weekend meetup and games shortcut rather than the old $162 Brooklyn sample. No CSS, JavaScript, navigation, page copy outside that alt text, policy text, support link or CNAME changed.

## Verification and delivery

- Local source check: 9/9 copied image hashes match the final App Store manifest and 9/9 HTML image URLs match the new hashes; the local HTTP server returned the exact nine hashes and HTTP 200 for `/`, `/privacy/` and the legacy policy route; `git diff --check` passed. At `http://127.0.0.1:4175/`, the browser showed the same home text/structure and the new first three slide artwork at the existing desktop viewport. The accessibility tree shows nine gallery images and the updated slide 03 description. This is a bounded visual check of the visible gallery, not a full responsive screenshot sweep.
- Git commit `1551684a7606866be5880c749ae9545082f57fd0` was pushed to `master` and `codex/app-introduction`. The legacy Pages build did not start automatically, so its build API was triggered; it completed `built` for this exact commit at 2026-09-23 10:31:40 UTC. The public `https://app.spendlab.net/` HTML references the nine new hash-versioned image URLs, and all nine public PNG SHA-256 values match the final en-US App Store package. `/privacy/` and `/spend/2019/01/30/privacy-policy.html` returned HTTP 200. Public readback verifies bytes and HTML references; visual preview was local only.

## What to Test

1. Open `https://app.spendlab.net/` and move horizontally through the nine screenshots. Slide 01 should show the Sydney trip and a games action in the transaction toolbar; slide 03 should show the $90 weekend meetup, slide 05 should show balances without a settlement games action, and slide 07 should show the currency picker chevron.
2. Reload once and confirm all nine images still load in 01–09 order. The page text, theme switch, App Store link and `/privacy/` should keep their prior behavior.
