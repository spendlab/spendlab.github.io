# SPEND privacy website work tracker

Updated: 2026-09-05

- COMPLETE — First independent website deployment, required before app production submission. Replace the old blog with Korean/English privacy content only. Preserve CNAME and the existing `/spend/2019/01/30/privacy-policy.html` link, add `/privacy/`, and make the root show the policy. Use plain HTML/CSS and no tracking or third-party page assets. User approved contact `cs@spendlab.net`; no legal operator identity has been supplied, so use the SPEND brand without inventing one.
- Baseline: clean master at `7faa728`; origin `https://github.com/spendlab/spendlab.github.io.git`; Pages publishes master `/`; push permission verified. Separate from the concurrently edited app repository.
- Inspection: old policy states that nothing is sent to a server. Current Flutter source includes optional exchange-rate requests and user-directed exports/support. No telemetry SDK appears in current pubspec. Describe existing behavior only; planned telemetry remains outside this delivery.
- Verification pending: local routes/assets/language behavior and mobile constraints; publication and public route checks. Review source before commit/push. Preserve history for rollback.
- USER REVISED — Design-only first pass: preserve the existing 2019 policy substance and date; add a Korean translation and the approved contact. One minimal modern page, no expanded legal policy or additional product pages. The known old-policy accuracy issue remains an app pre-production follow-up, not a claim that this visual refresh updates the policy for the unreleased Flutter app.

- Implementation complete: minimal responsive page, Korean/English hash links, approved email, original English wording/date, two compatibility routes. All old blog files preserved; `_config.yml` excludes historical blog surfaces from publication. Auto-review rejected bulk file deletion; no deletion was executed. Verification next.
- USER APPROVED — Explicit approval to delete obsolete blog files received after auto-review rejection. Remove old theme/build/posts/pages while retaining Git history, license and CNAME. Use direct static Pages publishing with `.nojekyll` instead of the temporary Jekyll exclusion configuration.

- Cleanup complete: 134 obsolete tracked blog/build files removed with explicit approval; `.nojekyll` enables static publishing. No app files changed. Verification next.
- Source verification PASS: original English policy text retained exactly after whitespace normalization; Korean/English sections and local asset references present; both compatibility routes lead to root; CNAME unchanged and `.nojekyll` present. `node --check language.js` and `git diff --check` passed. Local root responds HTTP 200. Preview opened for user. No new app-policy claims added.
- USER CLARIFIED — No need to preserve the old implementation technology; use a simple contemporary approach. Keep the completed standards-based HTML/CSS page and tiny language script: no framework/build dependency is needed for one static policy page. This does not expand the design-only scope.
- Verification PASS: local HTTP 200 for root, stylesheet, language script, `/privacy/`, and the historical policy URL. Language execution check passed default Korean, English/Korean switching, unknown-hash fallback, document language and active-link accessibility. JavaScript syntax and diff hygiene passed. Preview opening was queued by the app (not a confirmed browser rendering). Responsive CSS uses a constrained fluid page and narrow-screen rules; no screenshot/browser-layout QA claimed. Ready for commit and push.
- Commit complete: `4e60258` contains the verified website delivery. Working tree was clean afterward. Push and public deployment verification next.
- Push complete: origin/master advanced from `7faa728` to `5863764d6dc7b60e736edc16dda6b411da3a8878`. GitHub Pages reports that exact commit `building`, no build error.
- [CODEX-DISCOVERED] GitHub push response reports 5 dependency alerts (1 critical, 4 high). The old Gemfile/lockfile were removed in this delivery; alert clearance has not been verified. Queue independent repository-alert confirmation; do not expand this design delivery into a dependency project.
- Deployment verified: GitHub Pages reports `built` for `5863764`, with no build error. Public HTTPS root, CSS, JS, `/privacy/` and historical policy URL all match local source; root comparison accounts only for existing Cloudflare email obfuscation and its same-origin decoder. Both encoded contacts decode to approved `cs@spendlab.net`. Python urllib was rejected with 403; curl succeeded. Existing HTTP still serves 200 without HTTPS redirect; unchanged domain enforcement remains part of the separate pre-production hosting follow-up. No Cloudflare settings changed.
- COMPLETE — First design-only website delivery is public at https://blog.spendlab.net/. Original policy text/date preserved, Korean translation and approved contact added, minimal static design deployed, legacy routes verified. Policy accuracy for the upcoming Flutter app remains outside this completed visual refresh. Hosting email protection means JavaScript-disabled public visitors may see an encoded contact; local source itself needs no JavaScript for reading both policies.

- COMPLETE — USER REQUESTED dark-default theme and app branding. Current independent website deployment; cosmetic, safe after app production. Add dark/light display with dark as default, compare SPEND typography with the current app, remove the wordmark dot, and add `hi, spender` beneath it. Preserve the one-page scope and policy text. Inspect app typography read-only; do not modify the concurrent app work.
- Typography confirmed: app `SpendGreeting.launchTextStyle` uses Geist via shadcn_ui default family, size 34, weight 600, letter spacing 0. Existing web logo used system font/800/negative tracking and was not equivalent. Bundle the same app Geist font with its OFL license; set wordmark to the app style and use the user-requested lowercase `hi, spender`. Dark is default on each page load; add a small light/dark toggle without stored preferences.

- Implementation complete: default dark palette, light toggle with localized accessible labels, matching Geist/600/34px/zero-tracking SPEND wordmark, dot removed, lowercase greeting added. Font and license hosted locally. Header wraps on narrow screens; policy unchanged. Verification next.
- USER ADDED — Include the existing app icon as the browser favicon in this same branding deployment. Reuse shipped icon assets; do not design a new icon or add pages.

- Favicon implementation complete: copied exact iOS AppIcon PNGs (80px favicon, 180px Apple touch icon), with explicit HTML links. No image generation or modifications.
- Verification PASS: JS syntax/diff hygiene; executed dark default, light/dark toggling, metadata color, localized toggle labels, and independent language/theme state. Both policy articles unchanged byte-for-byte. Bundled font and both icons match app source bytes. Source uses wrapping header layout for narrow screens.
- USER ASKED license review: verified official OFL terms permit website font embedding/distribution; Geist copyright and full OFL included at `fonts/OFL.txt`, unmodified font preserved. Existing theme MIT notice remains at `LICENSE.md`. Icons are reused app-owned assets, not newly sourced third-party artwork; original app-icon ownership was not independently audited. No new runtime library or external asset service added.
- Commit complete: `335f671` contains the verified dark theme, app branding, favicon and font notices. Push/public verification next.
- Push complete: origin/master is `8811740f5c7cb6c8b7c128dad6d62730121b6812`; GitHub Pages is building this exact commit without errors.
- [CODEX-DISCOVERED] Current deployment blocker: Pages built `8811740`, new root is live, but unversioned CSS is cached at the previous design. Verified `?v=335f671` CSS/JS match source exactly. Add those version references to HTML so existing browser/CDN caches cannot combine new markup with old styles/scripts; no runtime scope expansion.
- Cache correction complete: root references verified versioned CSS/JS. Diff hygiene passes. Commit/push this final deployment correction.
- COMPLETE — Branding deployment verified. Runtime commit `335f671`; cache correction committed/pushed as `c7c938208fa898c5ed8816bb641ed8dba99fee45`, Pages reports built with no error. Public root confirms default dark, greeting, favicon and versioned asset URLs. Public versioned CSS/JS, both icons, font and OFL match local bytes. No browser-layout screenshot QA claimed. All requested branding changes and license review complete.

- COMPLETE — USER REQUESTED branding correction, current website deployment; cosmetic and safe post-production. Make SPEND heavier, use the app spelling `Hi, SPENDER`, enlarge the greeting so it extends beyond SPEND, round the favicon corners like the app, and set the browser title to exactly SPEND in either language. Preserve policy content and existing theme behavior. Use a native SVG clipping wrapper around the unchanged app PNG for rounded browser icon rendering.

- Implementation complete: Geist SPEND weight 750; `Hi, SPENDER` 26px/600; SVG favicon clips the exact existing 80px app PNG with 18px corners; page and language-switch titles are SPEND. Versioned CSS/JS references refreshed. Verification next.
- USER ADDED — Replace visible dark/light button text with sun/moon icons in this same deployment. Keep localized accessible labels, dark default and existing toggle behavior.

- Theme icon implementation complete: inline native sun/moon SVG icons, 44px button, theme-based visibility, localized aria-label and tooltip. No icon library or asset dependency.
- Verification PASS: JS syntax/diff hygiene; runtime checks for fixed SPEND title in Korean/English, dark/light transitions, localized icon aria-labels/tooltips. Policy article bytes unchanged. SVG parses and embeds the exact app PNG with rounded clipping. Brand styles use 34px/750 SPEND and 26px/600 Hi, SPENDER (longer text line); no screenshot/browser measurement claimed.
- Commit complete: `22bfe6d` contains verified typography correction, rounded browser icon, fixed tab title and icon-only theme control. Push/deployment next.
- Push complete: origin/master `24dfd6a1e6f2d90541cb36ba949e811828cc9c96`; Pages building exact commit with no error. New favicon SVG URL and versioned branding2 CSS/JS avoid old asset cache reuse.
- USER REVISED — English-only page. Remove Korean policy and language navigation, keep the existing English policy and all branding/theme requests. Old #ko/#en links must still show the English page. Fold into current deployment; supersedes bilingual presentation only.

- English-only implementation complete: Korean article/navigation removed, theme.js replaces language state, English accessibility/metadata/redirect copy, original English policy unchanged. Old URL hashes no longer affect content. Verification next.
- Verification PASS: English article matches previous source exactly, only one article, no Korean text/navigation, English document language, fixed SPEND title and default dark. Runtime theme toggle/English labels pass; JS syntax and diff hygiene clean. Rounded favicon/wordmark verification from prior unchanged source remains valid.
- English-only commit complete: `824c2f6`; push/deployment verification next.
- Push complete: origin/master `264aab3f584c093b541c7d966d06197d4cc80d36`; Pages building exact commit with no error. This final English-only version includes all current branding requests.
- COMPLETE — Final English-only branding deployment verified: Pages built `264aab3` with no error. Public root has one English article, no Korean, SPEND title, Hi, SPENDER, SVG favicon and versioned theme.js; public CSS/theme.js/favicon.svg match source bytes. All current branding, icon-only theme and English-only requests complete.

- IN PROGRESS — USER REVISED header balance: remove Hi, SPENDER and retain SPEND left / theme icon right. Current cosmetic website deployment; safe post-production. English-only remains unconditional including old #ko links; no language controls or state.
- Implementation and source verification complete: greeting and unused CSS removed, one-wordmark header preserved, stylesheet URL versioned. English policy/theme unchanged, no hash-dependent code. Diff hygiene passed.
- Commit complete: `ef713ae` is the verified header-only simplification. Push/public verification next.
